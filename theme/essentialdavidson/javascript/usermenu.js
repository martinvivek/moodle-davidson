require(['core/first'], function() { // jshint ignore:line
    require(['theme_essential/usermenu', 'core/log'], function(um, log) { // jshint ignore:line
        log.debug('Essential JavaScript Usermenu initialised');
    });
});
